# 📋 TaskManager API

RESTful API для управления задачами с JWT аутентификацией, ролевым доступом, файловыми вложениями и структурированным логированием.

## ✨ Возможности

- 🔐 **JWT аутентификация** - Безопасная авторизация с токенами
- 👥 **Управление пользователями** - Регистрация, роли (Admin/User), профили
- 📝 **Управление задачами** - CRUD операции, статусы, приоритеты, теги
- 📎 **Файловые вложения** - Загрузка файлов до 100MB через GridFS
- 🎯 **CQRS паттерн** - Разделение команд и запросов
- 📊 **Structured Logging** - Serilog с ротацией файлов
- 📚 **Swagger/OpenAPI** - Интерактивная документация
- 🐳 **Docker** - Готовая контейнеризация

## 🏗️ Архитектура

```
TaskManager/
├── Domain/          # Доменные модели
├── Contracts/       # DTO и контракты API
├── Application/     # Бизнес-логика (CQRS)
├── Infrastructure/  # БД и внешние сервисы
└── API/            # REST контроллеры
```

**Паттерны:** CQRS, Repository, Unit of Work, Mediator (MediatR), Dependency Injection

## 📚 API Примеры

### Регистрация
```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "Password123!",
    "name": "Иван Иванов",
    "role": "user"
  }'
```

### Вход
```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "Password123!"
  }'
```

### Создание задачи
```bash
curl -X POST http://localhost:5000/api/tasks \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Новая задача",
    "description": "Описание задачи",
    "status": "todo",
    "priority": "high",
    "dueDate": "2024-12-31",
    "tags": ["важное", "срочное"],
    "assignedUserIds": ["user-id-1", "user-id-2"]
  }'
```

### Получение задач
```bash
curl -X GET http://localhost:5000/api/tasks \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 📊 Логирование

Приложение использует **Serilog** для структурированного логирования:

- **Консоль** - Цветной вывод с эмодзи
- **Файлы** - Ежедневная ротация в папке `logs/`
- **Хранение** - 30 дней

**Пример логов:**
```
[10:30:20 INF] 🔐 Login attempt for user admin@test.com
[10:30:20 INF] ✅ User admin@test.com logged in successfully
[10:30:20 INF] HTTP POST /api/auth/login responded 200 in 145ms
```

## 🔒 Безопасность

- ✅ Хеширование паролей (BCrypt)
- ✅ JWT токены с HMAC-SHA256
- ✅ Ролевой доступ (RBAC)
- ✅ Валидация входных данных
- ✅ CORS конфигурация
- ✅ Защита от SQL инъекций
- ✅ Поддержка HTTPS

## 🛣️ Roadmap

- [x] CRUD операции для задач
- [x] JWT аутентификация
- [x] Файловые вложения
- [x] Ролевой доступ
- [x] Структурированное логирование
- [ ] Unit тесты
- [ ] Email уведомления
- [ ] Real-time обновления (SignalR)
- [ ] Комментарии к задачам
- [ ] Экспорт данных (CSV, PDF)
- [ ] Rate limiting
- [ ] Redis кеширование
